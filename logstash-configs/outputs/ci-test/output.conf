# CI Test Output - writes all events as JSON lines to a file
# Used by GitHub Actions E2E tests. No credentials required.

output {
  file {
    path => "/tmp/logstash-output/logstash-output.jsonl"
    codec => json_lines
  }
}
